Crear tabla:
CREATE TABLE IF NOT EXISTS empleado(legajo INTEGER GENERATED BY DEFAULT AS IDENTITY, nombre VARCHAR(100), apellido VARCHAR(100), dni INTEGER, direccion VARCHAR(100), honorarios DECIMAL(7,2), nombreUsuario VARCHAR(20), password VARCHAR(20));

Insertar:
insert into empleado(legajo, nombre, apellido, dni, direccion, honorarios, nombreUsuario, password) values (1111, 'juan', 'alfieri', 38893652, 'dean funes', 10, 'juan', 'asd');